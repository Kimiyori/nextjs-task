export type TNavLink = {
  url: string;
  img: { image: string; altName: string };
  name: string;
};
export type TtoDoCategories = {
  name: string;
  bgColor: string;
};

export type Data = {
  id: string;
  content: string;
  status: string;
};