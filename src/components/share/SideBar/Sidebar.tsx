import { FC } from 'react';
import SidebarWrapper, { StyledSideInfo } from '@components/share/SideBar/Sidebar.elements';

const Sidebar: FC = () => {
  return (
    <SidebarWrapper>
      <StyledSideInfo />
    </SidebarWrapper>
  );
};
export default Sidebar;
